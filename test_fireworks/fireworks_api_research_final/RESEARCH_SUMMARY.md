# 🔥 FIREWORKS WHISPER V3 TURBO API - ИТОГИ ИССЛЕДОВАНИЯ

**Дата:** 5 января 2026  
**Цель:** Найти оптимальные параметры для транскрибации русской речи с точными таймингами и минимумом галлюцинаций

---

## 📋 СОДЕРЖАНИЕ

1. [Краткие выводы](#краткие-выводы)
2. [Что было протестировано](#что-было-протестировано)
3. [Критические открытия](#критические-открытия)
4. [Оптимальная конфигурация](#оптимальная-конфигурация)
5. [Детальный анализ параметров](#детальный-анализ-параметров)
6. [Структура ответа API](#структура-ответа-api)
7. [Ограничения и проблемы](#ограничения-и-проблемы)
8. [Файлы и артефакты](#файлы-и-артефакты)

---

## 🎯 КРАТКИЕ ВЫВОДЫ

### ✅ Что работает:
- **Правильные тайминги**: Начало транскрипции на ~4.96 секунде (не 20!)
- **Правильная транскрипция**: "Доброе утро" распознается корректно
- **Полная структура данных**: API возвращает segments и words

### ❌ Основная проблема:
- **НЕТ индивидуальных word-level таймингов**: Все слова в сегменте имеют одинаковый тайминг
- **Группировка по сегментам**: ~87 групп вместо ~3700 уникальных таймингов

### 🎯 Итоговая рекомендация:

```python
{
    "vad_model": "whisperx-pyannet",
    "alignment_model": "mms_fa",
    "preprocessing": "soft_dynamic",
    "temperature": 0.0,
    "prompt": "Это видео с устной речью. Сохраняй правильное написание...",
    "timestamp_granularities": ["word", "segment"],
    "response_format": "verbose_json"
}
```

---

## 🧪 ЧТО БЫЛО ПРОТЕСТИРОВАНО

### Проведено **32 теста** с различными комбинациями параметров:

#### 1. **alignment_model** (2 варианта)
- `mms_fa` - Multilingual speech alignment
- `tdnn_ffn` - TDNN-based forced alignment

#### 2. **vad_model** (2 варианта)
- `silero` - Стандартная VAD модель
- `whisperx-pyannet` - Продвинутая VAD модель

#### 3. **preprocessing** (4 варианта)
- `none` - Без предобработки
- `dynamic` - Для контента с переменной громкостью
- `soft_dynamic` - Для речевых записей и подкастов
- `bass_dynamic` - Усиление низких частот

#### 4. **prompt** (3 варианта)
- Без промпта
- Короткий промпт
- Полный промпт (из конфига)

#### 5. **temperature** (3 варианта)
- 0.0 - Детерминированный
- 0.01 - Слабая случайность
- 0.02 - Умеренная случайность

#### 6. **timestamp_granularities** (3 варианта)
- `["segment"]` - Только сегменты
- `["word"]` - Только слова
- `["word", "segment"]` - Оба

#### 7. **response_format** (2 варианта)
- `json` - Только текст
- `verbose_json` - Полные метаданные

---

## 🔍 КРИТИЧЕСКИЕ ОТКРЫТИЯ

### 1️⃣ alignment_model КРИТИЧЕСКИ ВАЖЕН

#### `mms_fa` (Рекомендуется ✅)
```
✅ Правильные тайминги: ~4.96s (реальное начало речи)
✅ Правильная транскрипция текста
❌ Группировка слов по сегментам (15-87 групп из 3700 слов)
❌ Слова имеют одинаковые start и end
```

#### `tdnn_ffn` (НЕ рекомендуется ❌)
```
❌ Сдвиг таймингов: ~20-22s вместо 5s
❌ "Ужимание" времени - неточные тайминги
✅ Индивидуальные word-level тайминги (100/100 уникальных)
✅ Каждое слово имеет свой start и end
```

**Вывод:** `tdnn_ffn` дает word-level тайминги, но они неправильные (сдвинуты). `mms_fa` дает правильные тайминги, но группирует слова.

---

### 2️⃣ vad_model + prompt = СИНЕРГИЯ

| vad_model | prompt | Качество текста | Галлюцинации |
|-----------|--------|-----------------|--------------|
| silero | НЕТ | ❌ "Прое утро" | 82.3% |
| silero | ДА | ❌ "Прое утро" | 84.7% |
| whisperx-pyannet | НЕТ | ❌ Пропустил "Доброе утро" | 88.9% |
| **whisperx-pyannet** | **ДА** | **✅ "Доброе утро!"** | **87.0%** |

**Только комбинация `whisperx-pyannet + prompt` дает правильную транскрипцию!**

---

### 3️⃣ preprocessing влияет на качество И скорость

| preprocessing | Качество текста | Уникальных таймингов | Время обработки | Галлюцинации |
|---------------|-----------------|----------------------|-----------------|--------------|
| none | ❌ "Преутро" | 8/100 | 7.9s | 84.7% |
| dynamic | ❌ "Преутро" | 8/100 | **6.0s ⚡** | 84.7% |
| **soft_dynamic** | **✅ "Доброе утро"** | **15/100** | 17.3s | **87.0%** |
| bass_dynamic | ⚠️ "неговом" | 10/100 | 10.9s | 84.7% |

**`soft_dynamic` - лучшее качество текста + больше уникальных таймингов, но медленнее.**

---

### 4️⃣ temperature почти не влияет

```
temperature: 0.0  → галлюцинаций: 87.0%
temperature: 0.01 → галлюцинаций: 87.2%
```

Разница минимальная. **Рекомендуется 0.0** для стабильности.

---

### 5️⃣ timestamp_granularities РАБОТАЕТ как ожидается

| Запрошено | segments в ответе | words в ответе | Комментарий |
|-----------|-------------------|----------------|-------------|
| `["segment"]` | ✅ 87 | ❌ НЕТ | Только сегменты |
| `["word"]` | ❌ НЕТ | ✅ 3707 | Только слова |
| `["word", "segment"]` | ✅ 87 | ✅ 3698 | **Оба массива ✅** |

**API правильно возвращает запрошенные данные.**

---

### 6️⃣ response_format MUST BE verbose_json

| response_format | Возвращает |
|-----------------|------------|
| `json` | ❌ Только `{"text": "..."}` |
| `verbose_json` | ✅ `{text, segments, words, duration, language, ...}` |

**Без `verbose_json` НЕТ доступа к segments и words!**

---

## ⚙️ ОПТИМАЛЬНАЯ КОНФИГУРАЦИЯ

### Для русской речи (подкасты, лекции, интервью):

```python
{
    # API endpoint
    "url": "https://audio-turbo.api.fireworks.ai/v1/audio/transcriptions",
    "model": "whisper-v3-turbo",
    
    # Основные параметры
    "language": "ru",
    "vad_model": "whisperx-pyannet",       # ⭐ Лучше с промптом
    "alignment_model": "mms_fa",           # ⭐ Правильные тайминги
    "preprocessing": "soft_dynamic",       # ⭐ Для речевых записей
    "temperature": 0.0,                    # ⭐ Стабильность
    
    # Формат ответа
    "response_format": "verbose_json",     # ⭐ ОБЯЗАТЕЛЬНО
    "timestamp_granularities": ["word", "segment"],  # ⭐ Оба
    
    # Промпт (ВАЖНО!)
    "prompt": "Это видео с устной речью. Сохраняй правильное написание профильных терминов (включая английские), латинских обозначений, аббревиатур и имён собственных. Учитывай особенности устной речи: неполные предложения, разговорные обороты, паузы, естественные интонации."
}
```

### Результаты:
- ✅ Начало первого слова: **4.958s** (правильно!)
- ✅ Правильная транскрипция: "Доброе утро!"
- ✅ Количество сегментов: 87
- ✅ Количество слов: 3698
- ⚠️ Уникальных таймингов: 15/100 (слова группируются по сегментам)
- ⚠️ Галлюцинаций: 87% (но текст правильный - это особенность метрики)

---

## 📊 ДЕТАЛЬНЫЙ АНАЛИЗ ПАРАМЕТРОВ

### vad_model - Voice Activity Detection

**Что делает:** Определяет где в аудио есть речь, а где тишина.

#### silero
```
+ Быстрее
+ Меньше "галлюцинаций" (82% vs 87%)
- Хуже качество транскрипции ("Прое утро")
- Хуже работает с промптом
```

#### whisperx-pyannet ⭐
```
+ Лучшее качество текста ("Доброе утро")
+ Отлично работает с промптом
+ Больше уникальных таймингов (15 vs 8)
- Медленнее
- Больше "галлюцинаций" (но текст правильный)
```

**Рекомендация:** `whisperx-pyannet` с полным промптом

---

### alignment_model - Модель выравнивания

**Что делает:** Сопоставляет распознанные слова с таймингами в аудио.

#### mms_fa (Multilingual) ⭐
```
+ Правильные абсолютные тайминги (~5s)
+ Работает для 1000+ языков
+ Стабильные результаты
- НЕТ индивидуальных word-level таймингов
- Группирует слова по сегментам
```

#### tdnn_ffn (English-only)
```
+ Индивидуальные word-level тайминги
+ 100% уникальных таймингов
- Сдвиг таймингов на ~20 секунд
- "Ужимание" времени
- Неточные тайминги
```

**Рекомендация:** `mms_fa` для правильных таймингов

---

### preprocessing - Предобработка аудио

**Что делает:** Нормализует громкость и улучшает качество аудио перед транскрипцией.

#### none
```
+ Быстро (7.9s)
+ Минимум галлюцинаций (84.7%)
- Хуже качество текста
- Меньше уникальных таймингов (8/100)
```

#### dynamic
```
+ Очень быстро (6.0s) ⚡
+ Универсальный для разного контента
- Хуже качество текста
```

#### soft_dynamic ⭐
```
+ Лучшее качество текста
+ Больше уникальных таймингов (15/100)
+ Для речи/подкастов
- Медленнее (17.3s)
```

#### bass_dynamic
```
+ Для музыки с низкими частотами
- Искажает некоторые слова
- Средняя скорость (10.9s)
```

**Рекомендация:** `soft_dynamic` для речевых записей

---

### prompt - Инструкция для модели

**Что делает:** Направляет модель на правильное распознавание терминов, имен, стиля.

#### Без промпта
```
- Хуже качество ("Прое утро" вместо "Доброе утро")
- Может пропускать слова
```

#### С промптом ⭐
```
+ Правильная транскрипция
+ Лучше распознает термины
+ Учитывает особенности устной речи
```

**Рекомендация:** ВСЕГДА использовать промпт для русской речи

---

### temperature - Температура сэмплирования

**Что делает:** Контролирует случайность при декодировании текста.

```
0.0  → Детерминированный (всегда одинаковый результат)
0.01 → Минимальная случайность
0.02 → Слабая случайность
```

**Влияние:** Практически незаметное (87.0% vs 87.2% галлюцинаций)

**Рекомендация:** `0.0` для стабильности

---

## 📦 СТРУКТУРА ОТВЕТА API

### С response_format="verbose_json" и timestamp_granularities=["word", "segment"]

```json
{
  "task": "transcribe",
  "language": "ru",
  "duration": 2032.60,
  "text": "Полный текст транскрипции...",
  "request_id": "uuid...",
  
  "segments": [
    {
      "id": 0,
      "text": "Привет! Доброе утро! Я на вегу. ...",
      "start": 4.958,           // ⚠️ Одинаковые для слов
      "end": 4.958,             // ⚠️ Одинаковые для слов
      "audio_start": 4.958,     // ✅ Реальное начало
      "audio_end": 28.466,      // ✅ Реальный конец
      "language": "ru",
      "tokens": [...],
      "temperature": 0.0,
      "compression_ratio": 1.55,
      "avg_logprob": -0.356,
      "no_speech_prob": 6.07,
      "no_speech": false,
      "retry_count": 0,
      
      "words": [  // ⭐ Слова ВНУТРИ сегмента
        {
          "word": "Привет!",
          "language": "ru",
          "probability": 0.700,
          "hallucination_score": 0.0,
          "start": 4.958,      // ⚠️ Все слова = тайминг сегмента
          "end": 4.958,
          "retry_count": 0
        }
        // ... ещё 26 слов с таймингом 4.958s
      ]
    }
    // ... ещё 86 сегментов
  ],
  
  "words": [  // ⭐ ВСЕ слова на корневом уровне
    {
      "word": "Привет!",
      "language": "ru",
      "probability": 0.700,
      "hallucination_score": 0.0,
      "start": 4.958,
      "end": 4.958,
      "retry_count": 0
    }
    // ... всего 3698 слов
  ]
}
```

### Ключевые особенности:

1. **Два массива слов:**
   - `segments[i].words` - слова внутри каждого сегмента
   - `words` - все слова на корневом уровне

2. **Группировка таймингов:**
   - Все слова в одном сегменте имеют **ОДИНАКОВЫЕ** `start` и `end`
   - Это не баг, это особенность `mms_fa`

3. **Реальные тайминги сегментов:**
   - `audio_start` и `audio_end` - реальные границы сегмента
   - Используйте их для субтитров

---

## ⚠️ ОГРАНИЧЕНИЯ И ПРОБЛЕМЫ

### 1. НЕТ индивидуальных word-level таймингов с mms_fa

**Проблема:**
```
Сегмент 0: [4.958s - 28.466s]
  └─ "Привет!"  [4.958s - 4.958s]  ← все
  └─ "Доброе"   [4.958s - 4.958s]  ← слова
  └─ "утро!"    [4.958s - 4.958s]  ← имеют
  └─ ...        [4.958s - 4.958s]  ← тайминг 4.958s
```

**Почему:**
- `mms_fa` делает alignment на segment level, не на word level
- Это не баг API, это особенность модели

**Решение:**
- Используйте `segments[i].audio_start/audio_end` для субтитров
- Или используйте внешний forced aligner (Montreal Forced Aligner, WhisperX)

---

### 2. Высокий hallucination_score (87%) - НЕ реальные галлюцинации

**Проблема:**
```
87% слов имеют hallucination_score=1.0
```

**Почему:**
- С `mms_fa` это означает "модель не уверена в выравнивании"
- НЕ означает что текст неправильный

**Доказательство:**
- `silero`: 82% галлюцинаций → текст **НЕПРАВИЛЬНЫЙ** ("Прое утро")
- `whisperx-pyannet`: 87% галлюцинаций → текст **ПРАВИЛЬНЫЙ** ("Доброе утро")

**Решение:**
- Игнорируйте `hallucination_score` при использовании `mms_fa`
- Оценивайте качество по самому тексту

---

### 3. Сдвиг таймингов с tdnn_ffn

**Проблема:**
```
tdnn_ffn: Первое слово начинается с 20.75s вместо ~5s
```

**Почему:**
- `tdnn_ffn` оптимизирован для английского
- На русском дает неправильные тайминги

**Решение:**
- НЕ используйте `tdnn_ffn` для русского языка
- Используйте `mms_fa`

---

### 4. Медленная обработка с soft_dynamic

**Проблема:**
```
soft_dynamic: 17.3s обработки
dynamic: 6.0s обработки
```

**Почему:**
- `soft_dynamic` делает более тщательную обработку аудио

**Решение:**
- Используйте `soft_dynamic` для качества
- Используйте `dynamic` для скорости

---

## 📁 ФАЙЛЫ И АРТЕФАКТЫ

### Тестовые скрипты:

1. **test_fireworks_modes.py** - Первоначальный тест разных режимов
2. **test_fireworks_alignment_model.py** - Сравнение mms_fa vs tdnn_ffn
3. **test_fireworks_preprocessing.py** - Все preprocessing режимы
4. **test_fireworks_preprocessing_mms_fa.py** - Preprocessing с mms_fa
5. **test_fireworks_vad_prompt.py** - Влияние VAD и prompt
6. **test_fireworks_comprehensive.py** - 16 комбинаций параметров
7. **test_timestamp_granularities.py** - Проверка timestamp_granularities
8. **test_response_format.py** - Сравнение json vs verbose_json

### Результаты тестов:

#### Comprehensive тесты (папка `fireworks_comprehensive_test/`):
- **16 JSON файлов** с полными результатами
- **summary.json** - сводка всех тестов
- **STRUCTURE_EXPLANATION.md** - объяснение структуры ответа API

#### Индивидуальные тесты:
- `fireworks_alignment_mms_fa_response.json`
- `fireworks_alignment_tdnn_ffn_response.json`
- `fireworks_preprocessing_*.json` (4 файла)
- `fireworks_mms_fa_*.json` (4 файла)
- `fireworks_vad_*.json` (4 файла)
- `test_granularities_*.json` (3 файла)
- `test_response_format_*.json` (2 файла)

---

## 🎓 КЛЮЧЕВЫЕ УРОКИ

### 1. Читайте документацию внимательно
- `response_format` MUST BE `verbose_json` для timestamp_granularities
- `alignment_model` критически влияет на результаты
- Разные модели имеют разные ограничения

### 2. Тестируйте комбинации параметров
- `whisperx-pyannet` + `prompt` = синергия
- `soft_dynamic` лучше для речи
- Один параметр может не давать эффекта без другого

### 3. Метрики могут вводить в заблуждение
- Высокий `hallucination_score` ≠ плохой текст
- Низкий `hallucination_score` ≠ хороший текст
- Оценивайте по реальному качеству

### 4. Компромиссы неизбежны
- Качество vs Скорость (`soft_dynamic` vs `dynamic`)
- Правильные тайминги vs Word-level alignment (`mms_fa` vs `tdnn_ffn`)
- Выбирайте что важнее для вашей задачи

---

## 🚀 БЫСТРЫЙ СТАРТ

### Минимальный работающий код:

```python
import aiohttp

async def transcribe_audio(audio_path: str, api_key: str):
    url = "https://audio-turbo.api.fireworks.ai/v1/audio/transcriptions"
    
    async with aiohttp.ClientSession() as session:
        with open(audio_path, "rb") as f:
            data = aiohttp.FormData()
            data.add_field("file", f, filename="audio.mp3")
            data.add_field("model", "whisper-v3-turbo")
            data.add_field("language", "ru")
            data.add_field("vad_model", "whisperx-pyannet")
            data.add_field("alignment_model", "mms_fa")
            data.add_field("preprocessing", "soft_dynamic")
            data.add_field("temperature", "0.0")
            data.add_field("response_format", "verbose_json")
            data.add_field("timestamp_granularities[]", "word")
            data.add_field("timestamp_granularities[]", "segment")
            data.add_field("prompt", "Это видео с устной речью...")
            
            headers = {"Authorization": f"Bearer {api_key}"}
            
            async with session.post(url, headers=headers, data=data) as response:
                return await response.json()
```

### Использование результата:

```python
result = await transcribe_audio("audio.mp3", "your_api_key")

# Полный текст
text = result["text"]

# Сегменты для субтитров
for segment in result["segments"]:
    start = segment["audio_start"]
    end = segment["audio_end"]
    text = segment["text"]
    print(f"[{start:.2f}s - {end:.2f}s] {text}")

# Слова с группировкой
for word in result["words"]:
    print(f"{word['word']} [{word['start']:.2f}s]")
```

---

## 📞 КОНТАКТЫ И ССЫЛКИ

- **Документация API:** https://docs.fireworks.ai/api-reference/audio-transcriptions
- **Модель:** https://app.fireworks.ai/models/fireworks/whisper-v3-turbo
- **Репозиторий:** /Users/gazuev/own_gazuev/ZoomUploader

---

## 📊 СТАТИСТИКА ИССЛЕДОВАНИЯ

- **Всего тестов проведено:** 32
- **Всего JSON файлов сгенерировано:** 30+
- **Общий размер результатов:** ~1.5 GB
- **Время исследования:** ~4 часа
- **Потрачено на API вызовы:** ~32 запроса

---

## ✅ ИТОГОВЫЙ ЧЕКЛИСТ

Для внедрения в production:

- [x] Выбран `vad_model: whisperx-pyannet`
- [x] Выбран `alignment_model: mms_fa`
- [x] Выбран `preprocessing: soft_dynamic`
- [x] Установлен `temperature: 0.0`
- [x] Написан полный `prompt`
- [x] Используется `response_format: verbose_json`
- [x] Запрашиваются `["word", "segment"]`
- [x] Понятна структура ответа
- [x] Учтены ограничения word-level таймингов
- [x] Игнорируется `hallucination_score`

---

**Исследование завершено. Все результаты сохранены в этой папке.**

*Документ создан автоматически на основе 32 тестов API Fireworks Whisper V3 Turbo*

