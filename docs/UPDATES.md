# 📋 История обновлений

## Версия 0.7.2 (Текущая)

### 🎯 Режим извлечения тем
- Добавлен CLI флаг `--topic-mode` для команд `transcribe` и `full-process`
- Режимы: `short` (меньше тем, крупнее) и `long` (больше тем, детальнее, по умолчанию)
- Контроль детальности извлечения тем через `DeepSeek`

### 🚀 Улучшенный `full-process`
- Теперь работает с записями в любом статусе (`INITIALIZED`, `DOWNLOADED`, `PROCESSED`, `TRANSCRIBED`)
- Автоматически продолжает обработку частично обработанных записей
- Пропускает уже выполненные этапы (скачивание, обработка, транскрибация)
- Обновляет статусы после каждого этапа для корректной работы
- Улучшенная логика определения записей для загрузки (исключает уже загруженные)

### 🛠️ Рефакторинг CLI
- Вынесена общая логика фильтрации записей в функцию `_get_target_recordings()`
- Вынесена инициализация БД и pipeline в функцию `_setup_pipeline()`
- Упрощена команда `clean` - убраны неиспользуемые параметры (`from_date`, `to_date`, `account`, `config_file`)
- Унифицирована логика получения целевых записей во всех командах
- Улучшена обработка ошибок и логирование

### 📊 Обновления документации
- Обновлен `README.md` с новыми возможностями
- Добавлены примеры использования `--topic-mode`
- Обновлена таблица статусов обработки (добавлены `TRANSCRIBING` и `TRANSCRIBED`)

---

## Версия 0.7.1

### 🔥 Интеграция `Fireworks API`
- Добавлена поддержка `Fireworks Audio API` для транскрибации
- Модель `whisper-v3-turbo` используется по умолчанию
- Поддержка больших аудио файлов без разбиения на части
- Использование `alignment_model` для точных временных меток
- Динамический промпт с учетом контекста лекции

### 🔀 Переключение моделей транскрибации
- CLI флаг `--transcription-model` для выбора между `Fireworks` и `Whisper`
- `Fireworks` используется по умолчанию
- Полная совместимость форматов вывода

### 🧠 Улучшенное извлечение тем через `DeepSeek`
- Автоматическое определение перерывов (паузы ≥8 минут)
- Автоматическое добавление перерывов в список тем с временными метками
- Строгие правила: темы ≤12 минут (автоматическое разбиение длинных тем)
- Улучшенный промпт с учетом контекста (Computer Science, ML/Data Science, английские термины)

### 📋 Единый бизнес‑шаблон описания
- Раздел «🔹 Темы лекции:» с таймкодами
- Автоматическое добавление перерывов
- Служебная информация: дата публикации, P.S.

### 🛠️ Технические улучшения
- Единый стиль логов (`get_logger(__name__)`, `format_log`)
- Полировка линтинга и стабильности
- Улучшенная обработка ошибок и повторные попытки

---

## Версия 0.7.0

### 🎤 Интеграция `OpenAI Whisper`
- Добавлена поддержка транскрибации через `OpenAI Whisper API`
- Автоматическое разбиение больших файлов на части
- Нормализация результатов в единый формат

### 🧠 Интеграция `DeepSeek` для извлечения тем
- Автоматическое извлечение основных тем (2-3 слова)
- Создание детализированных тем с таймкодами (3-6 слов)
- Хронологическая структуризация контента

### 📝 Улучшенное форматирование описаний
- Единый формат таймкодов `HH:MM:SS — Название`
- Автоматическое формирование оглавления
- Поддержка различных платформ (YouTube, VK)

---

## Версия 0.6.0

### 🎬 Базовая функциональность
- Синхронизация записей из `Zoom API`
- Многопоточная загрузка видео файлов
- Обработка видео с обрезкой тишины
- Загрузка на `YouTube` и `VK`


### 🗄️ База данных
- `PostgreSQL` для хранения метаданных
- Система статусов обработки

### ⚙️ Конфигурация
- Поддержка нескольких `Zoom` аккаунтов
- Маппинг названий записей
- Настройки платформ

