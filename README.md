# 🎬 Zoom Publishing Platform

> **Автоматическое превращение записей Zoom в качественные публичные видео с AI-оглавлением и таймкодами**

Готовое решение для быстрого превращения записей `Zoom` в качественные публичные видео. Система автоматически забирает встречу из `Zoom`, обрезает «тишину», транскрибирует через AI (`Fireworks`/`Whisper`), извлекает темы через `DeepSeek`, добавляет AI‑метки глав с таймкодами, оформляет описание и публикует на `YouTube` и `VK` — за считанные минуты.

**Основная цель проекта:** автоматизация процесса публикации учебных лекций магистратуры с использованием современных AI-технологий для транскрибации и структурирования контента.

---

## 👥 Для кого это и какую ценность даёт

**Проект создан для автоматизации публикации лекций магистратуры «Искусственный Интеллект» (НИУ ВШЭ).**

- 🎓 **Учебные программы магистратуры**: оперативная публикация занятий с удобной навигацией по темам
- 📚 **Преподаватели и лекторы**: автоматизация процесса публикации учебного контента
- 🎯 **Студенты магистратуры**: быстрый доступ к структурированным лекциям с таймкодами
- 📢 **Команды контента**: быстрый конвейер «запись → оформленное видео → публикация»

### 🎯 Главные эффекты для магистратуры

- ⚡ **Быстрее время вывода контента**: `Zoom` → платформа без ручного монтажа
- 📈 **Выше вовлечённость студентов**: компактные видео без «пустых» участков и с кликабельным оглавлением
- 🎨 **Единый стиль публикаций**: описание и таймкоды оформляются по шаблону без ручных ошибок
- 🎓 **Автоматизация учебного процесса**: минимизация ручной работы при публикации лекций

## ✨ Что делает система автоматически

- 📥 **Забирает записи** из `Zoom` (по расписанию или по команде)
- ✂️ **Обрезает тишину** — удаляет пустое начало, конец и длинные паузы
- 🎤 **Транскрибирует аудио** через `Fireworks AI` (по умолчанию) или `OpenAI Whisper`
- 🧠 **Извлекает темы** через `DeepSeek`:
  - Определяет основные темы (2-3 слова)
  - Создает детализированные темы с таймкодами (3-6 слов, ≤12 минут)
  - Автоматически находит перерывы (паузы ≥8 минут)
- 📝 **Формирует описание**:
  - Раздел «🔹 Темы лекции:» с таймкодами `HH:MM:SS — Название`
  - Автоматическое добавление перерывов
  - Служебная информация: дата публикации, P.S.
- 🚀 **Публикует на платформы** — `YouTube` и `VK` с миниатюрами, плейлистами/альбомами
- 📊 **Ведёт журнал операций** и статусов, обрабатывает ошибки, поддерживает повторные попытки

## 🔄 Как это работает (бизнес‑уровень)

1. 🔌 **Подключение** `Zoom`‑аккаунтов и платформ (`YouTube`/`VK`)
2. 📥 **Автосбор** записей за выбранный период
3. ✂️ **Техническая чистка** видео (обрезка тишины, экспорт)
4. 🤖 **AI‑структурирование**: транскрибация (`Fireworks`/`Whisper`), темизация через `DeepSeek`, таймкоды, автоматическое определение перерывов
5. 🚀 **Автопубликация** с готовым описанием и миниатюрой

**Итог:** стабильно оформленные, готовые к просмотру видео появляются на платформах без ручного монтажа.

## 🆕 Что нового (последние обновления)

- 🔥 **Интеграция `Fireworks API`** для транскрибации с поддержкой больших файлов (без разбиения на части)
- 🔀 **Переключение моделей транскрибации** через CLI флаг `--transcription-model` (`fireworks`/`whisper`, по умолчанию `Fireworks`)
- 🎯 **Режим извлечения тем** через флаг `--topic-mode` (`short`/`long`): контроль детальности извлечения тем
- 🧠 **Улучшенное извлечение тем через `DeepSeek`** с автоматическим определением пауз/перерывов (≥8 минут)
- ⏸️ **Автоматическое добавление перерывов** в список тем с временными метками
- 📏 **Строгие правила AI‑меток**: темы ≤12 минут (автоматическое разбиение длинных тем), 3–6 слов
- 📋 **Единый бизнес‑шаблон описания** к видео (раздел «Темы лекции», таймкоды, перерывы, «Видео выложено…», P.S.)
- 🎓 **Улучшенный промпт для `DeepSeek`** с учетом контекста лекции (`Computer Science`, `ML`/`Data Science`, английские термины)
- 🚀 **Улучшенный `full-process`**: теперь работает с записями в любом статусе, автоматически продолжает обработку частично обработанных записей
- 🛠️ **Рефакторинг CLI**: вынесена общая логика фильтрации и инициализации, упрощена команда `clean`
- 🛠️ Единый стиль логов (`get_logger(__name__)`, `format_log`), полировка линтинга и стабильности

---

## 📚 Документация

- 📋 **[История обновлений](docs/UPDATES.md)** - версии 0.6.0, 0.7.0, 0.7.1
- 🔧 **[Техническая документация](docs/TECHNICAL.md)** - архитектура, модули, API
- 🚀 **[Руководство по развертыванию](docs/DEPLOYMENT.md)** - установка, настройка, production

## 🎛️ Команды

### 📋 Просмотр записей
```bash
python main.py list                              # Записи за сегодня
python main.py list --last 7                     # За последние 7 дней
python main.py list --from 2024-10-01            # С определенной даты
```

### 🔄 Синхронизация
```bash
python main.py sync                              # Синхронизация за 14 дней
python main.py sync --last 7                     # За 7 дней
```

### ⬇️ Загрузка видео
```bash
python main.py download --all                    # Все записи >30 мин
python main.py download --recordings "1,4,7"     # По ID
```

### ✂️ Обработка видео
```bash
python main.py process --all                     # Обработать все
python main.py process --recordings "1,4,7"      # По ID
```

### 🎤 Транскрибация
```bash
python main.py transcribe --all                                    # Транскрибировать записи (`Fireworks` по умолчанию)
python main.py transcribe --transcription-model whisper --all     # Использовать `OpenAI Whisper`
python main.py transcribe --transcription-model fireworks --all   # Явно указать `Fireworks`
python main.py transcribe --last 7                                # Транскрибировать записи за последние 7 дней
python main.py transcribe --all --topic-mode short                # Режим извлечения тем: меньше тем, крупнее
python main.py transcribe --all --topic-mode long                 # Режим извлечения тем: больше тем, детальнее (по умолчанию)
```

### 🚀 Загрузка на платформы
```bash
python main.py upload --youtube --all            # На `YouTube`
python main.py upload --all-platforms --all      # На все платформы
python main.py upload --youtube --all -i         # Интерактивно
```

### 🔄 Полный пайплайн
```bash
python main.py full-process --all                                    # Скачать + обработать + транскрибировать + загрузить
python main.py full-process --youtube --all -i                       # Интерактивно
python main.py full-process --all --transcription-model whisper      # Использовать `Whisper` для транскрибации
python main.py full-process --all --transcription-model fireworks    # Явно указать `Fireworks` (по умолчанию)
python main.py full-process --all --topic-mode short                 # Режим извлечения тем: меньше тем, крупнее
python main.py full-process --all --topic-mode long                 # Режим извлечения тем: больше тем, детальнее (по умолчанию)
python main.py full-process --all --no-transcription                # Пропустить транскрибацию (только скачать + обработать + загрузить)
```

> 💡 **Улучшение:** Команда `full-process` теперь работает с записями в любом статусе. Если запись уже частично обработана (например, уже скачана или обработана), пайплайн автоматически продолжит с нужного этапа, пропуская уже выполненные шаги.

### ⚙️ Управление
```bash
python main.py reset                             # Сбросить статусы
python main.py reset --full                      # Полная очистка: удалить все записи и видео файлы
python main.py clean --days 14                   # Очистить старые записи (старше 14 дней)
python main.py clean --days 7                    # Очистить старые записи (старше 7 дней, по умолчанию)
python main.py recreate-db                       # Полностью пересоздать БД (удалить и создать заново)
python main.py recreate-db --force                # Пересоздать БД без подтверждения
```

## 📊 Статусы обработки

| Статус | Описание |
|--------|----------|
| `INITIALIZED` | Инициализировано (загружено из Zoom API) |
| `DOWNLOADING` | В процессе загрузки |
| `DOWNLOADED` | Загружено |
| `PROCESSING` | В процессе обработки |
| `PROCESSED` | Обработано (готово к транскрибации или загрузке) |
| `TRANSCRIBING` | В процессе транскрибации |
| `TRANSCRIBED` | Транскрибировано (готово к загрузке) |
| `UPLOADING` | В процессе загрузки на платформу |
| `UPLOADED` | Загружено на платформу |
| `FAILED` | Ошибка обработки |
| `SKIPPED` | Пропущено |
| `EXPIRED` | Устарело (очищено) |

> 📖 Подробнее о статусах и процессе обработки см. [Техническую документацию](docs/TECHNICAL.md)



## 🛠️ Используемые технологии

### 🤖 AI и машинное обучение
- 🔥 **`Fireworks AI`** - транскрибация аудио через модель whisper-v3-turbo (по умолчанию)
  - 📖 [Документация Fireworks Audio API](https://fireworks.ai/docs/api-reference/audio-transcriptions)
- 🎤 **`OpenAI Whisper`** - альтернативная транскрибация через OpenAI API
- 🧠 **`DeepSeek`** - извлечение тем и структурирование контента через LLM

### 📦 Управление зависимостями и разработка
- ⚡ **`UV`** - быстрый пакетный менеджер для Python
- 🔍 **`Ruff`** - линтер и форматтер кода Python

### 🐍 Backend
- **`Python 3.8+`** - основной язык разработки
- **`asyncio`** - асинхронное программирование для эффективной обработки I/O
- **`aiohttp`** - асинхронный HTTP клиент
- **`SQLAlchemy 2.0+`** - ORM для работы с базой данных
- **`asyncpg`** - асинхронный драйвер PostgreSQL

### 🔌 API и интеграции
- **`httpx`** - современный HTTP клиент
- 📹 **`Zoom API`** - получение списка и загрузка записей
- 🎥 **`YouTube Data API v3`** - загрузка видео и управление плейлистами
- **`google-api-python-client`** - клиент для Google API
- **`google-auth`** - аутентификация Google
- **`google-auth-oauthlib`** - OAuth 2.0 для Google
- **`google-auth-httplib2`** - HTTP библиотека для Google Auth
- 📱 **`VK API`** - загрузка видео и работа с альбомами
- **`vk-api`** - клиент для VK API
- 🔐 **`OAuth 2.0`** - аутентификация с платформами
- 🔥 **`Fireworks Audio API`** - транскрибация аудио через модель whisper-v3-turbo
- **`fireworks-ai`** - официальный Python SDK для Fireworks API
- 🎤 **`OpenAI API`** - транскрибация через Whisper (альтернативный вариант)
- 🧠 **`DeepSeek API`** - извлечение тем и структурирование контента через LLM

### 🎬 Обработка видео
- **`ffmpeg-python`** - Python обертка для FFmpeg

### 🗄️ База данных
- **`PostgreSQL`** - реляционная БД для хранения метаданных
- **`Alembic`** - управление миграциями схемы БД

### 💻 CLI и UI
- **`Click`** - работа с аргументами командной строки
- **`Rich`** - красивое форматирование вывода и прогресс-бары

### 📝 Утилиты
- 📋 **`loguru`** - продвинутое логирование
- ✅ **`pydantic-settings`** - валидация конфигурации
- 🔧 **`python-dotenv`** - загрузка переменных окружения

## 🏗️ Архитектура

Система построена на модульной архитектуре:

- 📡 **`API` модуль** - работа с `Zoom API`
- ⬇️ **`Download` модуль** - многопоточная загрузка видео файлов
- ✂️ **`Processing` модуль** - обработка видео с `FFmpeg`
- 🎤 **`Transcription` модули**:
  - 🔥 **`Fireworks` модуль** - транскрибация через `Fireworks Audio API`
  - 🎤 **`OpenAI` модуль** - транскрибация через `OpenAI Whisper API`
  - 🧠 **`DeepSeek` модуль** - извлечение тем и структурирование контента
- 🚀 **`Upload` модуль** - загрузка на платформы
- 🗄️ **`Database` модуль** - управление состоянием
- 🎯 **`Pipeline Manager`** - координация всего процесса

## 📁 Структура проекта

```
├── api/                       # Zoom API клиент
├── config/                    # Конфигурация и учетные данные
├── database/                  # Модели и управление БД
├── models/                    # Модели данных
├── utils/                     # Утилиты и вспомогательные функции
├── video_download_module/     # Модуль загрузки видео
├── video_processing_module/   # Модуль обработки видео
├── openai_module/             # Модуль транскрибации (Whisper) и координации
│   ├── transcription_service.py  # Основной сервис транскрибации
│   ├── audio_compressor.py      # Сжатие и подготовка аудио
│   └── config.py                 # Конфигурация OpenAI
├── fireworks_module/          # Модуль транскрибации через Fireworks
│   ├── service.py             # Сервис транскрибации Fireworks
│   └── config.py              # Конфигурация Fireworks
├── deepseek_module/           # Модуль извлечения тем через DeepSeek
│   ├── topic_extractor.py     # Извлечение тем и структурирование
│   └── config.py              # Конфигурация DeepSeek
├── video_upload_module/       # Модуль загрузки на платформы
│   ├── platforms/             # Платформы (YouTube, VK)
│   └── core/                  # Базовые классы
├── video/                     # Папки для видео
│   ├── unprocessed_video/     # Исходные видео
│   ├── processing/            # Видео в обработке
│   └── processed_video/       # Обработанные видео
├── thumbnails/                # Превью для видео
├── main.py                    # Основной скрипт
├── pipeline_manager.py        # Менеджер пайплайна
└── requirements.txt           # Зависимости
```

## 🚀 Быстрый старт

### 1️⃣ Установка зависимостей

Рекомендуется использовать UV:

```bash
# Установка UV (если еще не установлен)
curl -LsSf https://astral.sh/uv/install.sh | sh

# Установка зависимостей
uv sync
```

Или используйте pip:

```bash
pip install -r requirements.txt
```

### 2️⃣ Настройка конфигурации

Создайте файлы с учетными данными в папке `config/`:

| Файл | Описание | Обязательно |
|------|----------|-------------|
| `zoom_creds.json` | `Zoom API` | ✅ |
| `youtube_creds.json` | `YouTube API` | ✅ |
| `vk_creds.json` | `VK API` | ✅ |
| `fireworks_creds.json` | `Fireworks AI` (транскрибация) | ✅ |
| `deepseek_creds.json` | `DeepSeek` (извлечение тем) | ✅ |
| `openai_creds.json` | `OpenAI Whisper` (альтернатива) | ⚪ |

### 3️⃣ Инициализация базы данных

База данных создается автоматически при первом использовании. Если нужно пересоздать БД с нуля:

```bash
uv run python main.py recreate-db
# или
python main.py recreate-db
```

> ⚠️ **Внимание:** Команда `recreate-db` полностью удаляет базу данных и создает её заново. Все данные будут потеряны!

### 4️⃣ Синхронизация записей

```bash
uv run python main.py sync --last 7
```

### 5️⃣ Полный пайплайн обработки

```bash
# Скачать + обработать + транскрибировать + загрузить
uv run python main.py full-process --all
```

## 📋 Требования

### ✅ Обязательные

- 🐍 **Python 3.8+** - основной язык разработки (рекомендуется 3.11+)
- 🎬 **FFmpeg** - обработка и конвертация видео
  ```bash
  brew install ffmpeg  # macOS
  sudo apt install ffmpeg  # Ubuntu
  ```
- 🗄️ **PostgreSQL 12+** - база данных для хранения метаданных
  - Установка: [PostgreSQL Downloads](https://www.postgresql.org/download/)
- 🔑 **API ключи**:
- 📹 `Zoom API`
- 🎥 `YouTube Data API v3`
- 📱 `VK API`
- 🔥 `Fireworks AI` (транскрибация)
- 🧠 `DeepSeek` (извлечение тем)

### ⚪ Опциональные

- 🎤 **`OpenAI API` ключ** - для альтернативной транскрибации через `Whisper`
  - Регистрация: [OpenAI Platform](https://platform.openai.com/)
  - Требуется только если используется флаг `--transcription-model whisper`

### 🚀 Рекомендуемые (для производительности)

- ⚡ **`UV`** - для быстрой установки зависимостей
- 💻 **Многоядерный процессор** - для параллельной обработки видео
- 💾 **SSD накопитель** - для быстрого доступа к видео файлам
- 🌐 **Стабильное интернет-соединение** - для загрузки/выгрузки видео
- 🧠 **Не менее 4 ГБ RAM** - для обработки нескольких видео одновременно

### ✨ Система поддерживает

- 🔄 **Многопоточность** - одновременная загрузка и обработка нескольких видео
- ⚡ **Асинхронные операции** - эффективная работа с I/O операциями
- 📊 **Мониторинг прогресса** - отслеживание статуса обработки в реальном времени

## ⚙️ Конфигурация

Все файлы конфигурации должны быть размещены в папке `config/`. 

**Необходимые файлы:**
- `zoom_creds.json` - `Zoom API`
- `youtube_creds.json` - `YouTube API`
- `vk_creds.json` - `VK API`
- `fireworks_creds.json` - `Fireworks AI` (транскрибация) ✅ обязательно
- `deepseek_creds.json` - `DeepSeek` (извлечение тем) ✅ обязательно
- `openai_creds.json` - `OpenAI Whisper` (опционально)
- `app_config.json` - основная конфигурация

> 📖 Подробные инструкции по настройке всех конфигураций см. в [Руководстве по развертыванию](docs/DEPLOYMENT.md)
